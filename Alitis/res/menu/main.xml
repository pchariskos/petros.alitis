<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:alitis="http://schemas.android.com/apk/res-auto"
    tools:context="petros.alitis.MainActivity" >
    
        <item
        android:id="@+id/action_search"
        android:title="@string/action_search"
        alitis:showAsAction="always"
        alitis:actionViewClass="android.support.v7.widget.SearchView"
        android:icon="@drawable/ic_action_search"/>
    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_action_refresh"
        android:title="@string/action_refresh"
        alitis:showAsAction="always"/>

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        alitis:showAsAction="never"
        android:title="@string/action_settings"/>

</menu>
